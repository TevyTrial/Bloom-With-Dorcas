using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class LocationManager : MonoBehaviour
{/*
    public static LocationManager Instance{ get; private set; }

    public List<StartPoint> startPoints;

    private void Awake()
    {
        // if there is more than one instance, destroy the new one
        if (Instance != null && Instance != this)
        {
            Destroy(this);
        }
        else
        {
            // Set the instance to this
            Instance = this;
        }
    }

        // Find the player's start point based on where they are entering from
        public Transform GetPlayerStartingPosition(SceneTransitionManager.Location enteringForm) {
            // Tries to find the matching start point
            StartPoint startPoint = startPoints.Find(sp => sp.enteringFrom == enteringForm);

            //Return the transform
            return startPoint.playerStart;
        
    }*/
}
